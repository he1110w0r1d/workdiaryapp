# 数据恢复与总结访问修复

基于 `8f8b17f`，覆盖原审查的六项问题。

## 恢复行为

- JSON 恢复必须包含当前用户的 metadata.userId，以及 diaries、todos、summaries 三个数组；空集合用 `[]` 明确表示。
- 删除前校验所有记录、设置、记录归属及关联；拒绝重复 ID、缺失集合、悬空关联和跨用户数据。暂不支持跨账号迁移。
- 重新分配记录 ID 并改写双向关联。总结 HTML 路径视为缓存，不从备份恢复，查看时重新生成。
- 在数据库事务中读取原数据，并在 `backend/backup/backup-用户ID-pre-restore-UUID.json` 保存恢复前快照，然后执行替换和用户设置保存。快照写入失败会终止恢复。
- `strategy` 仍仅控制用户设置：merge 合并模型配置，overwrite 替换模型配置，skip 跳过设置；三个业务集合都恢复为备份内容。
- 必须使用支持事务的 MongoDB 副本集或分片集群。不支持时返回 503，原数据不变。仓库默认 docker-compose 的 MongoDB 是独立实例，因此默认环境会拒绝真实恢复；本次没有自动迁移数据库或修改线上配置。
- 原有备份清理任务会删除超过七天的备份，包括恢复前快照。快照包含个人内容及模型配置，应按私有备份保管。
- ZIP 增强恢复仍仅提供预检，没有开放真实导入。

## 总结和上传

- 只公开 `/uploads/avatars`，其余 `/uploads` 路径返回 404。
- 新 HTML 写入 `backend/private/summaries`，每次生成使用 UUID，避免跨周期及并发生成覆盖。Docker 已加入该目录的持久卷。
- `/api/summaries/:id/html` 同时验证登录和记录归属，并返回禁止缓存的沙箱页面。前端携带登录凭证获取内容，在独立沙箱 iframe 中展示。
- 老文件保留在原目录但应用不再提供访问；查看旧记录会重新生成私有 HTML。已经被覆盖的旧 HTML 无法从旧文件名恢复。
- 部署前须确认反向代理没有独立公开旧的 `/uploads/summaries` 或整个 uploads 目录；本次未检查线上代理。
- 上传仅挂载在三个需要文件的接口，顺序为鉴权、限流、上传、控制器。限制单文件 50 MB、每用户每进程 15 分钟最多 10 次，响应结束清理临时文件。多进程部署的全局限流需代理或共享存储支持。
- HTML 提示词按用户保存；公共默认模板保持只读，同时补上 weekly 提示词的模型字段。

## 验证

```powershell
cd backend
npm ci
npm test
```

默认执行本地单元及 HTTP 回归测试；真实副本集测试默认跳过。指定专用测试数据库连接后，可验证真实事务回滚：

```powershell
$env:TEST_MONGODB_URI = 'mongodb://127.0.0.1:27017/?replicaSet=rs0'
node --test tests/restore.integration.test.js
```

集成测试仅使用显式指定的连接，不读取生产 `.env`；在连接上创建随机命名的 `workdiary_restore_test_*` 数据库，结束时仅删除该测试库及该测试用户的快照。请使用独立测试副本集。

前端验证：在 frontend 执行 `npm ci` 和 `npm run build`。此次仅抽出恢复服务、HTML 文件服务与访问控制；日期计算、模型调用和定时任务的大规模拆分留待后续。

本次本地验证结果（2026-09-16）：17 项测试通过，真实副本集集成测试因未配置测试数据库而跳过；前端生产构建通过，仍有未使用变量、Hook 依赖、Browserslist 数据陈旧和包体积提示。`git diff --check` 及修改后控制器的 Node 语法检查通过。未进行浏览器交互验收、线上代理检查或服务器部署。
