# 网络安全配置说明

## 局域网访问限制

本项目已配置为仅允许局域网192.168.1.x网段访问，提高安全性。

### 配置详情

#### 后端限制
- **IP访问控制**: 只允许192.168.1.x网段和本地(127.0.0.1)访问
- **服务绑定**: 后端服务绑定到192.168.1.84:5000
- **拒绝策略**: 非授权IP访问将返回403错误

#### 前端限制
- **服务绑定**: 前端服务绑定到192.168.1.84:3000
- **API配置**: 前端API请求指向192.168.1.84:5000

### 启动服务

1. **启动后端**:
   ```bash
   cd backend
   npm start
   ```
   服务将在 http://192.168.1.84:5000 启动

2. **启动前端**:
   ```bash
   cd frontend
   npm start
   ```
   服务将在 http://192.168.1.84:3000 启动

### 访问方式

- **局域网内设备**: 可通过 http://192.168.1.84:3000 访问
- **本机访问**: 可通过 http://localhost:3000 或 http://127.0.0.1:3000 访问
- **其他网段**: 将被拒绝访问

### 安全特性

1. **IP白名单**: 只允许指定网段访问
2. **请求日志**: 记录所有访问请求的IP地址
3. **拒绝日志**: 记录被拒绝的访问尝试
4. **代理支持**: 正确处理反向代理环境下的真实IP

### 自定义配置

如需修改允许的网段，请编辑以下文件：

1. **后端IP限制** (`backend/server.js`):
   ```javascript
   // 修改正则表达式中的网段
   const ipv4Regex = /^192\.168\.1\.(\d{1,3})$/;
   ```

2. **服务绑定地址** (`backend/server.js`):
   ```javascript
   app.listen(PORT, '192.168.1.84', () => {
   ```

3. **前端配置** (`frontend/.env`):
   ```
   HOST=192.168.1.84
   REACT_APP_API_URL=http://192.168.1.84:5000/api
   ```

### 注意事项

- 确保您的网络接口配置了192.168.1.84地址
- 如果无法绑定到192.168.1.84，请检查网络配置
- 防火墙可能需要允许相应端口的访问
- 在Docker环境中可能需要额外的网络配置

### 故障排除

1. **无法启动服务**:
   - 检查192.168.1.84是否为本机IP
   - 确认端口3000和5000未被占用

2. **无法访问**:
   - 检查客户端是否在192.168.1.x网段
   - 查看服务器日志中的IP访问记录

3. **恢复默认配置**:
   - 将HOST改为0.0.0.0或删除HOST配置
   - 移除后端IP限制中间件