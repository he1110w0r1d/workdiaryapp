# 响应式设计更新待办

目标：提升手机竖屏下的可用性，避免内容拥挤与错位；确保顶部导航可访问，侧边栏不占用主屏空间；日历在移动端以“日程视图”展示。

## 侧边栏（移动端抽屉式）
- [ ] 默认在小屏隐藏侧边栏，使用抽屉滑出方式显示
- [ ] 添加打开/关闭抽屉的状态与过渡动画
- [ ] 在桌面端（≥md）保持固定侧边栏

## 顶部导航（TopBar）
- [ ] 左侧加入汉堡菜单按钮，控制抽屉侧边栏
- [ ] 极小屏收缩次要元素（帮助/退出文字），仅保留图标
- [ ] 顶部导航使用粘性定位，保证随内容滚动仍可访问

## 日历（核心修复）
- [ ] 桌面保持“月视图网格”布局
- [ ] 移动端切换为“日程视图”（列表）：每个日期作为卡片显示关联工作日志
- [ ] 列表项支持跳转到对应日期的日记页或新建日志

## 整体布局与样式
- [ ] 主内容区域可滚动，避免侧边栏与内容竞争空间
- [ ] 调整全局样式，保证canvas、图表等在移动端不被缩放影响交互

## 预览与验证
- [ ] 在服务器 `192.168.1.168` 上预览前端，确认抽屉、顶部栏与日程视图行为正确
- [ ] 交互测试：菜单开关、页面滚动、日历列表跳转、新建日志入口

## 发布与回滚
- [ ] 更新PM2前端进程并保存
- [ ] 如出现问题，回滚到上一个稳定版本或关闭抽屉特性开关

备注：前端运行端口 `13000`，后端 API 端口 `5000`。