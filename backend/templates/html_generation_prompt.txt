你是一名网页设计师。请基于“总结正文”生成页面的主要内容与结构，并使用“统计数据”渲染图表与数据卡片。不要引用或展示原始日记文本。

页面与图表生成的强制约束（务必严格遵守）：
1) 布局与样式
- 使用现代、简洁的样式，包含栅格卡片与淡入动画，但动画只执行一次，不得导致持续布局变更。
- 必须为图表容器设置固定高度，避免因尺寸反馈循环造成页面无限增长：
  - `.charts-section { display: grid; grid-template-columns: repeat(auto-fit, minmax(400px, 1fr)); gap: 24px; }`
  - `.chart-container { background: rgba(255,255,255,0.95); border: 1px solid #e5e7eb; border-radius: 12px; box-shadow: 0 10px 30px rgba(99,102,241,0.1); padding: 20px; position: relative; }`
  - `canvas` 不要设置 CSS 高度/宽度，避免非等比缩放导致饼图变形；仅需 `canvas { display: block; }`

2) 图表库与配置（Chart.js 或等价库）：
- 允许使用 Chart.js UMD（示例：`https://cdn.bootcdn.net/ajax/libs/Chart.js/4.4.0/chart.umd.min.js`）。
- 必须设置 `responsive: true`，并保持纵横比：
  - 柱状图：`maintainAspectRatio: true` 并设定 `aspectRatio: 2`（宽:高=2:1）。
  - 环形/饼图：`maintainAspectRatio: true` 并设定 `aspectRatio: 1`，确保图形为正圆，避免椭圆。
- 禁止使用会导致持续尺寸变化的配置：
  - 不要使用 `maintainAspectRatio: false`，避免出现因容器尺寸不确定导致的高度增长与 tooltip 误位。
  - 不要对 `canvas` 或其父容器施加 `scale` 变换；页面的淡入动画仅用于非图表容器（如章节与卡片），避免影响鼠标坐标计算。
  - 不要编写 `setInterval`、循环 `update()` 或任何会不断改变图表尺寸/数据的逻辑。
- 只需要一次性渲染，无需持续动画；淡入动画可用于卡片和章节，但不得触发布局回流循环。

3) 结构与内容
- 页面包含：头部（标题与报告周期/概要信息）、统计卡片（总时长、条目数、日均时长、工作天数等）、两张图表（每日工作时长条形图、工作类型分布环形图）、正文内容（本周概述、详细工作、亮点、问题与方案、下周计划）。
- 使用“统计数据”中的字段驱动卡片与图表；图表的 `labels` 与 `data` 来自统计数据对象，不要改写数据口径。

4) 兼容性与可读性
- 使用系统常见字体，保证在不同设备显示良好。
- 保持清晰的语义标签与样式类名，避免过度嵌套。

请输出完整的可直接打开的 HTML 文件（包含 `<style>` 与 `<script>`），并严格执行以上约束，确保页面在响应式下稳定，不出现图表高度不断增大的问题。

页面要求：
1. 现代化设计，响应式布局，蓝紫色配色
2. 包含：标题、数据概览卡片、Chart.js图表、详细内容
3. 日期处理：使用getFullYear()和getMonth()+1正确显示月份
4. 引入Chart.js（优先使用国内CDN，如 BootCDN 或 Staticfile；不可用时内联必要脚本）。如需图标，避免外部CDN，优先使用内嵌SVG或内联样式。
5. 直接返回完整HTML，包含所有CSS和JavaScript；不要引用海外CDN链接。

统计数据（仅用于图表与指标）：
{{summaryData}}

总结正文（页面主要内容来源）：
{{summaryContent}}

**重要：直接返回完整HTML代码，从<!DOCTYPE html>开始到</html>结束，不要任何说明文字或markdown标记，也不要使用markdown代码块围栏。**