# 生产发布与 UI 调整交接

2026-09-16 已将两轮操作流程改进及真实联调修复部署到 `192.168.1.168`。功能代码提交为 `f21ec97`；后续发布记录提交仅修改文档。入口：`http://192.168.1.168:13000`。

## 发布结果

- MongoDB 已从独立实例迁移到带认证、keyfile 的单节点副本集 `workdiary`，继续使用原数据卷。应用 URI 已配置副本集，原登录密钥、加密配置和个人模型设置保留。
- 演练及生产使用 MongoDB 7.0.39，原实例为 7.0.28；演练验证了认证、事务提交/回滚及回到独立模式。部署覆盖文件固定 MongoDB、PostgreSQL 镜像 digest，避免后续随可变标签漂移。
- 首次切换北京时间 22:47:15–22:47:24，约 9 秒。之后两次后端小修复切换，前端保持运行。生产 API 和前端最终均返回 HTTP 200。
- 627 条现有日记全部完成索引，808 个 4096 维向量片段；逐条核对日记内容版本、模型配置版本及切片前缀，全部一致。重启中断的后台任务通过租约恢复完成，最终无失败或运行中任务。
- 原有日记、待办、总结、用户配置和旧 Mongo 嵌入集合，与备份时内容哈希一致。API key 的凭据/权限未变，一条 key 的 `lastUsedAt` / `usageCount` 随正常调用变化。最后验证旧报告 HTML 时只更新该报告的缓存路径，正文未变。
- 2 个临时验收账号、关联日记/待办/总结/任务/索引、2 个测试备份文件、3 个测试 HTML 文件均已清理；隔离演练容器、数据卷和凭据副本已清理。

## 验证

- 51 项自动化测试全部通过，无跳过，使用隔离 MongoDB 副本集与真实 PostgreSQL pgvector。
- 生产 API 使用合成账号验证注册登录、日记保存和归属隔离、真实总结任务、私有 HTML 访问控制、建议重复确认只创建一条待办。
- 生产备份恢复验证：非法备份拒绝且原数据保留；合法 JSON 通过事务替换并保存恢复前快照，恢复后的日记/待办关联完整；未登录上传返回 401。
- 验证个人 HTML 提示词不会改变另一个用户的提示词；不同周报的 HTML 路径不同。
- 浏览器验证新版工作台、任务状态、恢复后的总结和已确认建议、真实问答和来源链接、空日期范围不回退到其他日记、退出登录。
- 前端本机生产构建通过，使用原 Nginx 运行镜像封装构建产物；服务器 npm 下载较慢的构建已停止。原有 lint/Browserslist 警告仍在。

验收中额外修复：

1. 删除报告或恢复备份后，历史任务不再提供失效的报告链接，而是提示去总结列表查看。
2. 旧报告和新报告一样，从保存的正文生成私有 HTML。不会因读取现在的日记或重新调用模型而改变历史报告内容。

## 运行与回滚材料

- 源码：`/home/shenchun/文档/personal-portal/workdiaryapp/workdiaryapp`。原有未跟踪的 `deploy.sh` 保留。
- 运行配置/证据：`/home/shenchun/workdiary-release-20260916`。含密钥的文件仅保存在服务器受限目录，不提交 Git。
- 生产目录的 `docker-compose.override.yml` 链接到该目录的 `compose.production.json`，包含已固定的镜像、keyfile 挂载、持久化模型配置目录和副本集 URI。默认 `docker compose` 会读取此覆盖文件；不要只用基础 compose 文件替换当前部署。
- 完整备份及恢复演练：`/home/shenchun/workdiary-backups/20260916-222348`。
- 停写后最新快照：`/home/shenchun/workdiary-backups/20260916-224645-preupgrade`。保存发布前 Mongo/PG/应用文件，以及另外命名的发布后私有部署配置归档（包含 keyfile），并有 SHA-256 清单。
- `compose.rollback.json` 记录发布前镜像和应用环境。数据库恢复前先停写并备份当时数据；不要为回退应用而直接覆盖上线后的新增记录。旧前后端镜像另保存在完整备份中。
- 主要证据：`acceptance-result.json`、`additional-acceptance.json`、`final-regression.tap`、`final-release-check.json`、`original-data-final-verification.json`、构建和部署日志。

## UI 调整起点

核心工作流已具备线上使用基础，可以开始讨论页面布局、信息层级、颜色、排版及移动端体验。当前视觉沿用原样。

保留的边界：Dify 仍未配置/启用，不影响站内向量问答；备份正式恢复支持 JSON，ZIP 仍仅预检；单节点副本集提供事务，不代表多机高可用；备份尚在同一服务器。默认总结提示词仍带主观工作评价，提示词语气与事实约束可和产品体验一起继续调整；本次没有进行整个旧控制器的大规模拆分或依赖升级。
