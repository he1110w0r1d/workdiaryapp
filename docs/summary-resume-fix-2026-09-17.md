# 总结断点恢复与重试修复（2026-09-17）

提交 cd08ccd、f706f05 已部署本地与阿里云。备份目录分别为 /home/shenchun/workdiary-resume-lan-20260917/backup 与 /root/workdiary-resume-release-20260917/backup，离机归档保存在受限 tmp/aliyun-release/private。

- 按提示词摘要在每个任务内保存成功模型结果，租约校验后立即落库。失败、人工重试和重启后复用成功分段；成功完成后清理中间缓存。
- 总结请求关闭模型调用层的瞬时重试和截断扩容重试，避免与后台任务重试相乘。瞬时失败至多执行两次任务尝试；超时与明确参数/认证错误直接停止并给出操作提示。
- 尊重用户设置的超时，不再强制至少五分钟。错误提示区分超时、模型拒绝、限流、连接失败；不暴露模型原始响应或凭据。
- 分段进度显示当前段/总段数。任务预算分别为整理 8192、正文 16384、建议 4096，并受用户配置最大值约束；不修改用户全局最大输出设置。
- 隔离副本集测试 64 通过，1 个 pgvector 专项跳过。真实数据库测试覆盖中途超时、旧进度复用、只保存一个最终总结、成功后清理缓存。

本地原 8 月任务 6aab7c5e693fa5a6028bde9b 在部署后原地重试，未重复创建任务。旧实现没有持久化的已完成片段无法恢复，36 条原始日记快照保留。运行中发现一次 EAI_AGAIN open.bigmodel.cn；随后域名解析恢复。模型速度和供应商可用性仍受外部服务影响，断点恢复不等于保证模型能完成。

真实恢复验证：第一次遇到短暂 DNS 解析失败，有限重试后准确显示 30 秒模型超时。随后仅将本地任务所属用户默认 glm-5.3-flash 的 timeout 从 30000 调为 180000（原配置已在部署前备份），保持最大输出和其他配置不变，恢复同一任务。该变更是明确的运行配置调整，不是隐藏的代码超时下限。
