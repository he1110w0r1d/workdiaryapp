# 日记展示、日报与问答修复（2026-09-17）

## 问题与修复

- 日记详情原来直接输出字符串，导致 Markdown 标记外露。现在使用 react-markdown + GFM + sanitize；保留原始编辑文本，重新整理标签、地点、时间和优先级样式。
- 后台总结忽略用户配置，将输出固定为 4000 tokens，即使重试也只有 8000。现在使用用户配置的预算（本次实际默认配置为 32000），仍执行提供商上限和截断检查。网络瞬时重试不再削减完整报告预算，完整输入不再被静默裁切。
- 问答服务器日志显示向量服务在 60000ms 超时。指定日期的问题直接按北京时间和左闭右开区间读取本人的有效日记，覆盖周期内全部记录；长资料分段整理。普通语义查询保留版本与归属校验，瞬时向量错误重试一次。回答模型使用与总结相同的用户默认模型，生成失败返回可操作错误，不再返回伪成功的占位答案。

## 验证

- 本地后端：28 项通过，4 项数据库集成测试跳过。
- 随后在服务器的随机命名隔离数据库和 PG schema 中运行这 4 组集成测试：27 项全部通过，0 跳过；自动清理测试库。
- 前端生产构建通过，仍有既有 ESLint / bundle 大小警告。
- 浏览器使用合成日记确认标题、列表、引用、表格正常渲染，script 不执行，元信息及标签样式统一。未修改用户日记内容。

## 部署

修复提交：497e0e0cfcfb9293a56783de61d8d4f478b337c3。
服务器：192.168.1.168（shenchun-linux）。
备份：`/home/shenchun/workdiary-backups/20260917-091640-before-ui`，包括 Mongo、PG、私有文件、前后端旧镜像与 compose 配置；压缩、PG 目录和 SHA256 校验通过。
发布记录：`/home/shenchun/workdiary-ui-release-20260917-fixes/DEPLOYED`。
前端：`workdiary-release-frontend:ui-497e0e0cfcfb`。
后端：`workdiary-release-backend:fix-497e0e0cfcfb`。

## 真实业务验收

2026-09-16 失败日报（任务 `6aaacb10d3ec1af705cf931d`）重试成功：正文 2337 字符，统计包含该日 2 条记录，任务状态 succeeded，并生成 10 条待确认建议。日记仍为 627 条、待办仍为 662 条；总结由 358 增为 359，未替换旧报告。

- “上周的主要工作是什么？”：真实 HTTP 200，success=true，答案 3708 字符，12 条来源，335951ms（约 5 分 36 秒）。
- 普通语义问答：真实 HTTP 200，success=true，答案 1655 字符，5 条来源，53026ms。
- 长周期问答需要多次模型调用，当前默认模型下仍较慢；这是已确认的性能限制，不能将功能恢复描述为即时回答。
- 没有建立生产测试账号；以失败任务所属用户调用既有接口完成验收，未输出凭证或日记正文。

