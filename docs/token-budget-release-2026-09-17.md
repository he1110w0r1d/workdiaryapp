# 模型输出预算与长输入修复（2026-09-17）

功能提交 bd3f5fa。取消个人设置 32000、旧设置 8000 的表单上限；字段改为“最大输出 Token”，说明输出预算与上下文容量的区别。已有设置数值不自动变更。

移除按厂商/模型名称硬编码的 8192/16384/32768/65536 输出上限，以及 20000 字符输入截断和超时重试时的 12000 字符截断。瞬时错误保持原输入和输出预算重试；服务端拒绝参数时直接报告错误，不静默减半。输出完整性检查与一次扩容重试保留。

显式运维环境变量 EXTERNAL_LLM_MAX_OUTPUT_TOKENS_CAP 仍可用于预算控制，两台部署机器均未设置它。表单和模型 schema 保留正整数校验；应用无法取消模型服务自身的最大输出或上下文限制。长周期总结继续分段归并全部资料，没有把百万上下文当作百万输出。

Node 18 测试 36 通过，4 个数据库组跳过。新增验证 131072 输出预算、60000 字符输入以及重试输入完整性。两套前端构建通过（保留既有 lint/bundle 警告）。部署后临时账号实际保存/读取 131072 并核对预算不被压缩，未调用付费模型；临时账号清理。

本地和阿里云分别使用自己的前端 API 地址和运行配置，镜像构建在本机及 LAN 完成，云端不编译。镜像后缀 tokens-20260917。部署前备份与业务集合哈希核对保留。
备份：LAN /home/shenchun/workdiary-token-lan-20260917/backup；云 /root/workdiary-token-release-20260917/backup。
