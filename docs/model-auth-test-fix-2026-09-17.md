# 模型测试误退出登录修复（2026-09-17）

本地模型测试日志确认：上游模型服务返回 401，测试接口原样透传，前端全局 401 拦截器因此清除本站登录凭据。

修复：上游测试失败映射为 502（超时 504），401/403 显示模型认证提示，原状态保存在 upstreamStatus。网站本身的登录失效仍正常返回 401。未修改用户 API 密钥、模型配置或前端登录保护。

新增测试覆盖上游 401、403、429 与本地缺少密钥验证。Node 18 镜像测试 35 通过、4 个数据库组跳过。部署验收使用临时用户和本机模拟上游 401，无付费模型调用；失败后同一凭据访问总结接口仍为 200，未登录访问仍为 401。临时用户清理。

部署镜像：workdiary-aliyun-backend:authfix-20260917，功能提交 913bc52。构建在 LAN 进行，阿里云加载镜像。两台服务器均先备份后切换，保留各自配置和数据。
备份目录：LAN `/home/shenchun/workdiary-auth-lan-20260917/backup`；阿里云 `/root/workdiary-auth-release-20260917/backup`。

模型服务本身的认证失败尚需核对密钥、接口与访问权限；本次修复解决误退出登录，并不会使无效模型凭据变为有效。
