# 备份改进清单

目标：在不改变业务功能的前提下，提升备份/还原的完整性、一致性、安全性、可观测性与可运维性，并与 PM2 管理模式协同。

## 状态标记说明
- 已完成：功能已在后端落地并通过基本联测（plain v1）。
- 进行中：基础能力已落地，剩余增强项待补充。
- 占位：已有端点与基本骨架，核心逻辑待实现。
- 待实现：尚未落地。

## 备份模式与默认策略
- 默认模式：不脱敏（包含密钥与敏感配置，适用于个人单机场景）。
- 优点：备份/还原全自动，恢复后无需重新配置密钥或环境。
- 风险：备份包含敏感信息，请保持本地私密存储，避免外泄或误同步到云盘。
- 规划开关：后续提供“脱敏+加密”模式（使用 `ENCRYPTION_KEY` 生成 `secrets.enc`），跨设备/多人共享时可选用。

## 不脱敏模式操作步骤
- 备份内容：
  - `backend/config/llm-settings.json`（保留 `externalApiKey/externalEmbeddingsApiKey`）。
  - MongoDB 数据（含用户 `llmConfigs.apiKey`）。
  - 若使用 `.env` 提供敏感变量，建议连同 `.env` 一并备份。（已完成：导出参数 `includeEnv` 支持）
- 恢复步骤：
  - 将 `llm-settings.json` 还原至 `backend/config/`，还原数据库数据。
  - 保持 PM2 的 `ecosystem.config.js` 或 `backend/.env` 不变（或按备份回写）。
  - 执行 `pm2 restart all`，后端会重新加载配置（含密钥），恢复无需手动补齐。
- 还原后处理：
  - 自动触发 RAG 重索引与摘要 HTML 回填（按还原流程实现）。
  - 执行健康检查，确认 API/页面/RAG 问答正常。

## 安全建议（个人环境）
- 备份包存放在仅你可访问的位置（NTFS 权限限制，取消共享）。
- 优先使用加密磁盘（如 BitLocker）存储备份。
- 避免通过即时通讯/邮箱传输备份包；如需移动，优先使用本地移动硬盘。
- 在文件名或 README 中标注“含敏感信息”，防止误上传至云端。

## 覆盖范围扩展
- [ ] 纳入静态文件目录：`backend/uploads/summaries/`、`backend/uploads/avatars/`
- [ ] 纳入模板文件：`backend/templates/*.txt`
- [ ] 记录 `ecosystem.config.js` 的关键参数快照（非机密项），用于环境重建参考
- [ ] 生成 `.env` 脱敏快照（仅非密钥项），并在备份中附带敏感项清单提示需手动配置
- [ ] 为 RAG 嵌入索引提供策略：导出必要的索引数据或在还原后自动重建（优先后者）

## 归档与元数据
- [x] 采用压缩归档（`zip`）（已完成：plain v1 导出为 zip；分片与流式待实现）
- [ ] 提升或移除 `multer` 上传限制（当前 50MB），改为流式处理以支持大文件
- [ ] 写入备份元数据：备份时间、应用版本、数据库 schema 版本、时区（与 `server.js` 中 `Asia/Shanghai` 对齐）、校验和（SHA256）（进行中：已写入基础 `meta.json` 含时间/模式/参数与数据统计；校验和已在验证阶段返回，写入到 meta.json 待实现）
- [ ] 提供可选加密：口令或公钥加密（优先使用非对称公钥加密）

## 还原一致性
- [ ] 引入维护模式：通过 PM2 暂停定时任务与写入路由（对外只读/维护页），避免并发写污染
- [ ] 使用事务或两阶段导入：校验→导入→重映射→索引重建→切换；失败可回滚
- [ ] 实现 ID 重映射表：先导入主表（用户、日记），再导入从表（待办、摘要），修复外键引用
- [ ] 定义冲突解决策略（合并/覆盖/跳过）：基于自然键（如 `userId+startTime+title`）进行去重与匹配

## 派生数据重建
- [ ] 还原完成后，触发 `/rag/reindex` 做全量嵌入重建，确保检索可用且日期语义正确
- [ ] 检测并重生成缺失的摘要 HTML（根据 `Summary` 记录和模板重建）
- [ ] 清理与预热缓存（视项目中缓存使用情况而定）
- [ ] 统一时区配置与日期计算逻辑（与 `Asia/Shanghai` 保持一致），确保“昨天/上周”等相对时间解析准确

## 体验与可观测性
- [ ] 提供可视化进度与详细步骤日志（导入阶段、重映射阶段、重建阶段等）（进行中：后端进度查询端点与基础日志已提供）
- [x] 提供 `dry-run` 与 `validate` 端点：不改动数据，仅报告可行性与潜在冲突（已完成：plain v1 校验与干跑分析，返回 `checksum/issues/plan`）
- [ ] 详细错误报告与审计日志（谁在何时做了备份/还原）
- [ ] 还原完成后的健康检查与端到端验证：包括 API、UI、RAG 问答抽样的自动化测试

## 运维与自动化
- [ ] 在 PM2 中配置定时自动备份（每日/每周），附带保留策略（如 7/30 天滚动）
- [ ] 支持远程或 NAS 存储目标（SMB/NFS/对象存储），并提供恢复向导
- [ ] 备份前进行健康检查与容量/配额预检查，避免失败与半成品

## 路由与接口调整
- [x] 新增端点：`/api/backup/export`（已完成：plain v1）、`/api/backup/progress`（已完成：基础进度查询）；`/api/backup/validate`（已完成：plain v1 校验）与 `/api/backup/restore-enhanced`（进行中：dry-run 完成，真实导入暂未开放）
- [ ] 上传改为流式分片端点，提升大备份上传成功率与稳定性
- [ ] 加强权限控制与速率限制，确保安全与服务稳定

### 接口参数规划与示例（模式开关：不脱敏 / 脱敏+加密）

说明：以下为规划方案，作为实现参考与对外文档草案；默认模式为“不脱敏”。

- 备份导出：`POST /api/backup/export`
  - 当前实现（plain v1）：返回 JSON（`fileName/filePath/downloadUrl/progressId`），下载接口支持原始文件名（含 `.zip`）。
  - 导出包结构（plain v1）：
    - `meta.json`：包含备份时间、模式、参数、数据统计（diaries/todos/summaries 计数）。后续将加入版本/时区/校验和。
    - `data.json`：包含 `diaries[]`、`todos[]`、`summaries[]` 的原始记录。
    - 可选 `backend/config/llm-settings.json`：如文件存在则包含。
    - 可选 `.env`：当 `includeEnv=true` 时包含。
  - 请求体（JSON 或表单）：
    - `mode`: `plain` | `secure`（默认 `plain` 不脱敏；`secure` 为脱敏+加密）
    - `includeEnv`: `true|false`（是否包含 `backend/.env`）
    - `split`: `true|false`（是否按分片输出，便于大文件传输）
    - `chunkSizeMB`: 数值（当 `split=true` 时有效，建议 100-500）
  - 响应：
    - `application/zip` 或 分片流；当 `mode=secure` 时，包内包含 `secrets.enc`（AES-GCM 加密密钥材料）。
  - 示例（不脱敏）：
    ```bash
    curl -X POST http://localhost:5000/api/backup/export \
      -H "Content-Type: application/json" \
      -d '{"mode":"plain","includeEnv":true}' \
      --output backup_plain.zip
    ```
  - 示例（脱敏+加密）：
    ```bash
    curl -X POST http://localhost:5000/api/backup/export \
      -H "Content-Type: application/json" \
      -d '{"mode":"secure","includeEnv":true}' \
      --output backup_secure.zip
    ```

- 备份验证（不改动数据）：`POST /api/backup/validate`
  - 请求体：
    - `mode`: `plain` | `secure`
    - `source`: `upload` | `existing`（上传文件或引用服务器现有备份）
    - `backupFile`: `multipart/form-data`（当 `source=upload`，字段名为 `backupFile`）
    - `fileName`: 备份文件名（当 `source=existing`）
  - 响应：
    - 校验结果（`checksum/issues/plan`、结构与版本信息、是否包含 `llm-settings.json/.env`）。
  - 示例（上传校验）：
    ```bash
    curl -X POST http://localhost:5000/api/backup/validate \
      -F "mode=plain" -F "source=upload" -F "backupFile=@backup_plain.zip"
    ```
  - 示例（引用服务器备份校验）：
    ```bash
    curl -X POST http://localhost:5000/api/backup/validate \
      -H "Content-Type: application/json" \
      -d '{"mode":"plain","source":"existing","fileName":"backup-<userId>-<timestamp>.zip"}'
    ```

- 备份还原（增强接口）：`POST /api/backup/restore-enhanced`
  - 请求体：
    - `backupMode`: `plain` | `secure`（默认 `plain`）
    - `source`: `upload` | `existing`
    - `backupFile` / `fileName`: 上传文件或服务器备份文件名
    - `strategy`: `merge` | `overwrite`（合并或覆盖策略）
    - `dryRun`: `true|false`（先试跑，输出预期更改与冲突）
    - `encryptionKey`: 字符串（当 `backupMode=secure` 时必需，用于解密 `secrets.enc`）
  - 响应：
    - `dry-run` 模式下返回 `plan`（`total/overwrite/newItems/notes`）与 `progressId`；真实导入暂未开放。
  - 示例（不脱敏，覆盖，干跑）：
    ```bash
    curl -X POST http://localhost:5000/api/backup/restore-enhanced \
      -F "backupMode=plain" -F "source=upload" -F "strategy=overwrite" -F "dryRun=true" \
      -F "backupFile=@backup_plain.zip"
    ```

- 进度查询：`GET /api/backup/progress?id=<progressId>`
  - 响应：
    - `status`: `queued|running|completed|failed`
    - `stage`: `validate|import|remap|reindex|finalize`
    - `percent`: 0-100
    - `logs`: 最近关键日志摘要
  - 示例：
    ```bash
    curl "http://localhost:5000/api/backup/progress?id=abc123"
    ```

## 文档与操作指南
- [ ] 更新 `README-PM2.md`：维护模式切换、备份/还原步骤、自动备份配置与恢复流程
- [ ] 更新 `README.md`：环境变量说明、敏感项清单与配置策略、备份文件格式说明
- [ ] 更新 `LAN_SETUP.md`：备份上传的防火墙/Nginx 配置与端口策略
- [ ] 提供用户操作手册与故障排查清单（典型错误与处理路径）

## 代码落地路径（建议）
- [x] `backend/controllers/backupController.js`：归档/解包逻辑、校验与校验和（已完成：plain 导出 zip+meta+data；validate 校验与 checksum；restore-enhanced dry-run 计划分析）
- [x] `backend/routes/backup.js`：新增路由端点（export/progress/validate/restore-enhanced），权限沿用 `protect` 中间件
- [ ] `backend/server.js`：维护模式控制、还原完成后的健康检查与自动化验证触发
- [ ] `backend/utils/`：新增/完善 `compression`、`checksum`、`encrypt`、`progress` 工具方法
- [ ] `ecosystem.config.js`：定时自动备份任务与必要环境变量
- [ ] `frontend`（后续迭代）：备份/还原 UI、进度条与日志查看页面

## 验收标准（最低可行）
- [ ] 完整恢复后，RAG 问答能正确理解相对时间并仅引用有效日期范围内容
- [ ] 摘要页面均能加载对应 HTML 文件，无缺失或报错
- [ ] 日记/待办/摘要引用关系无孤儿或错位记录
- [ ] 支持大体量备份（>1GB，含静态文件）的上传与还原成功（可分片）
- [ ] 还原过程支持回滚；日志能定位到具体失败步骤；审计记录完整

---

备注：当前上传使用 `multer` 且限制 50MB，建议改为流式或分片上传；时区以 `Asia/Shanghai` 为准（见 `server.js` 定时任务），并在 RAG 提示中明确当前日期与时区，保证相对时间语义稳定。